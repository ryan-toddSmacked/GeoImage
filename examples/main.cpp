#include <iostream>
#include "GeoImage/GeoImage.hpp"

int main() {
    std::cout << "GeoImage Library Example\n";

    geoimage::GeoImage img;

    // Example usage (would need an actual TIFF file)
    // if (img.open("test.tif")) {
    //     std::cout << "Image size: " << img.width() << "x" << img.height() << "\n";
    //     img.close();
    // }

    return 0;
}
